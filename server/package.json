{
  "name": "clickdep-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "simple-git": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}